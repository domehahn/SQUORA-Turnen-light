assurance:
    file: assurance.yaml
compatible_with:
    - claude-code
    - github-copilot
    - codex
contract:
    file: contract.yaml
dependencies:
    file: dependencies.yaml
description: Review mirrord using its official documentation and repository in the App Definition and Development / Application Definition & Image Build category. mirrord is an open-source developer tool that runs local code as if it were a pod in a remote Kubernetes cluster, with real env vars, DNS, network, and inbound traffic. Lets engineers iterate on a single service locally while interacting with the rest of the cluster, and is also used by AI coding agents (Claude, Cursor, Codex).
entrypoint: SKILL.md
evals:
    directory: evals
goal:
    failure_conditions: []
    objective: Review mirrord using its official documentation and repository in the App Definition and Development / Application Definition & Image Build category. mirrord is an open-source developer tool that runs local code as if it were a pod in a remote Kubernetes cluster, with real env vars, DNS, network, and inbound traffic. Lets engineers iterate on a single service locally while interacting with the rest of the cluster, and is also used by AI coding agents (Claude, Cursor, Codex).
    success_criteria: []
integrations:
    a2a:
        file: integrations/a2a.yaml
    mcp:
        file: integrations/mcp.yaml
license: MIT
name: cncf-mirrord-reviewer
ownership:
    maintainers: []
    primary: platform-engineering
schema_version: "2"
version: 1.0.0
