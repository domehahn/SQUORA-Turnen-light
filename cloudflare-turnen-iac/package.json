{
  "name": "turnen-cloudflare-infrastructure",
  "private": true,
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "cf:deploy:api": "wrangler deploy",
    "cf:deploy:web": "wrangler pages deploy dist",
    "cf:types": "wrangler types"
  }
}
